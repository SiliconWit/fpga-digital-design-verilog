---
title: "Building a Mini CPU"
description: "Build a processor from the parts you now understand. Datapath, ALU, register file, program counter, and instruction decode for a tiny instruction set. An 8-bit CPU that executes a small program from memory."
contributors: "ian-kigen,sam-macharia"
pubDate: 2026-06-19
tags: ["verilog", "fpga", "cpu", "alu", "datapath", "instruction-set", "computer-architecture"]
excerpt: "A CPU is not magic, it is the blocks from this course wired together: registers, an ALU, memory, and a state machine that fetches and executes instructions. In this lesson you design a small 8-bit processor and watch it run a real program on the FPGA."
heroImage: "https://cdn.siliconwit.com/education/fpga-digital-design-verilog/building-a-mini-cpu.png"
---

import TawkWidget from '../../../../components/TawkWidget.astro';
import UniversalContentContributors from '../../../../components/UniversalContentContributors.astro';
import InArticleAd from '../../../../components/InArticleAd.astro';
import Copyright from '../../../../components/Copyright.astro';
import BionicText from '../../../../components/BionicText.astro';
import TailwindWrapper from '../../../../components/TailwindWrapper.jsx';
import { Tabs, TabItem } from '@astrojs/starlight/components';
import { Card, CardGrid, Badge, Steps, LinkButton, FileTree } from '@astrojs/starlight/components';

<UniversalContentContributors 
  contributors={frontmatter.contributors}
/>



import FpgaDigitalDesignVerilogComments from '../../../../components/fpga-digital-design-verilog/FpgaDigitalDesignVerilogComments.astro';

You have used the STM32 and ESP32 without ever seeing what executes your code. This lesson removes the mystery. A processor is a register file, an arithmetic unit, a memory, and a state machine that reads instructions and acts on them. You have built every one of those pieces. Now you will connect them into a working CPU. #verilog #cpu #computerarchitecture

{/* CONTRIBUTOR NOTE
   Goal: a complete, tiny, working CPU the reader can run a program on. This is the engagement peak
   of the course. Keep the ISA small (a handful of instructions). Keep all headings.
   Rules: no em dashes; titled code blocks; code out of <Steps>; no `$` for currency.
*/}

## Learning Objectives

By the end of this lesson, you will be able to:

1. **Define** a minimal instruction set for an 8-bit CPU.
2. **Design** the datapath: register file, ALU, program counter, and memory.
3. **Build** the control FSM that implements fetch, decode, and execute.
4. **Run** a small hand-assembled program and observe it on the FPGA.

## What We Are Building

<InArticleAd />


<Card title="An 8-bit CPU running a small program" icon="star">
  You will design a tiny processor with a handful of instructions (load, store, add, jump, and a branch), load a short program into its memory, and watch it execute on the board, for example counting on the LEDs.
</Card>

## Designing a Tiny Instruction Set

<InArticleAd />


{/* CONTRIBUTOR: define 6 to 8 instructions, their opcodes, and the instruction word format.
   Keep it small enough to fully implement and explain. Present the ISA as a table. */}

## The Datapath

<InArticleAd />


{/* CONTRIBUTOR: register file, ALU (reuse concepts from earlier), program counter, and the memory
   interface. A block diagram is very valuable here. */}

## The Control FSM: Fetch, Decode, Execute

<InArticleAd />


{/* CONTRIBUTOR: this is where the state-machine lesson pays off. Walk through the cycle and how the
   control signals steer the datapath. */}

```verilog title="cpu.v"
module cpu (
    input  wire clk,
    input  wire rst,
    output wire [7:0] debug_pc
);
    // CONTRIBUTOR: program counter, instruction register, register file, ALU, and the
    // fetch/decode/execute control state machine go here
endmodule
```

## Writing and Loading a Program

<InArticleAd />


{/* CONTRIBUTOR: hand-assemble a short program, load it via $readmemh into memory, and run it. */}

:::tip[Instrument it]
A CPU you built yourself is the perfect thing to make observable. Bring the program counter and a few control signals out to pins or LEDs so you can literally watch fetch, decode, and execute step through your program, this is the clearest debugging aid you will have. Driven slowly from a button, single-stepping the clock turns the abstract cycle into something you can see. The same exposed PC and status lines are what an [external monitor](https://siliconwit.io) would later read to confirm the core is alive and progressing.
:::

## Application Questions and Solutions

<InArticleAd />


### Question 1: The program counter never advances

After loading a program, the CPU sits with the program counter stuck at zero. Where do you look first?

<details>
<summary>**Click to reveal the solution**</summary>

<Steps>

1. **Check the control FSM.** A stuck program counter usually means the fetch state never asserts the signal that increments it, or the FSM is not leaving the reset or fetch state. ✅

2. **Confirm the clock and reset.** Verify the core is actually clocked and that reset is released. A held reset looks exactly like a stuck PC. ✅

3. **Trace one cycle in simulation** with a waveform, watching the state, the PC enable, and the instruction fetched, until the first increment happens. ✅

</Steps>

</details>

## Summary

<InArticleAd />


| Concept | Key Takeaway |
|---------|--------------|
| Instruction set | A small, fixed set of operations encoded in the instruction word |
| Datapath | Register file, ALU, program counter, and memory wired together |
| Control FSM | Fetch, decode, and execute steer the datapath with control signals |
| Program | Loaded into memory and executed one instruction at a time |

:::tip[Next Lesson]
In [Lesson 8: FPGA plus MCU Co-Design](/education/fpga-digital-design-verilog/fpga-mcu-codesign), you step back to the system level: when a problem belongs on an FPGA, when it belongs on a microcontroller, and how to make the two work together.
:::

<FpgaDigitalDesignVerilogComments />



<InArticleAd />
<TawkWidget />
<Copyright />